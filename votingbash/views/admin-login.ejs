<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Login - LINCSSA BASH</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
    <style>
      :root {
        --primary-gold: #dcca87;
        --accent-gold: #ffd700;
        --deep-space: #000810;
        --cyber-purple: #9d4edd;
      }

      body {
        background: var(--deep-space);
        color: #fff;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .login-container {
        background: rgba(0, 0, 0, 0.7);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(220, 202, 135, 0.3);
        border-radius: 25px;
        padding: 3rem 2rem;
        width: 100%;
        max-width: 420px;
        position: relative;
        overflow: hidden;
      }

      .login-container::before {
        content: "";
        position: absolute;
        inset: -10px;
        background: linear-gradient(
          45deg,
          transparent,
          var(--primary-gold),
          transparent
        );
        animation: borderGlow 6s linear infinite;
        z-index: -1;
        filter: blur(20px);
      }

      .login-header {
        text-align: center;
        margin-bottom: 2rem;
      }

      .login-header i {
        font-size: 3rem;
        color: var(--primary-gold);
        margin-bottom: 1rem;
      }

      h2 {
        color: var(--primary-gold);
        font-size: 2rem;
        margin-bottom: 0.5rem;
        text-shadow: 0 0 10px rgba(220, 202, 135, 0.3);
      }

      .form-control {
        background: rgba(255, 255, 255, 0.1);
        border: 1px solid rgba(220, 202, 135, 0.3);
        border-radius: 12px;
        color: #fff;
        padding: 0.8rem 1rem;
        margin-bottom: 1.5rem;
        transition: all 0.3s ease;
      }

      .form-control:focus {
        background: rgba(255, 255, 255, 0.15);
        border-color: var(--primary-gold);
        box-shadow: 0 0 15px rgba(220, 202, 135, 0.2);
        color: #fff;
      }

      .form-floating > label {
        color: rgba(255, 255, 255, 0.7);
        padding-left: 1rem;
      }

      .form-floating > .form-control:focus ~ label,
      .form-floating > .form-control:not(:placeholder-shown) ~ label {
        color: var(--primary-gold);
        transform: scale(0.85) translateY(-0.5rem) translateX(0.15rem);
      }

      .btn-login {
        background: linear-gradient(
          45deg,
          var(--primary-gold),
          var(--accent-gold)
        );
        border: none;
        border-radius: 12px;
        color: var(--deep-space);
        font-weight: 600;
        padding: 0.8rem 2rem;
        width: 100%;
        transition: all 0.3s ease;
      }

      .btn-login:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(220, 202, 135, 0.3);
      }

      @keyframes borderGlow {
        0%,
        100% {
          opacity: 0.5;
        }
        50% {
          opacity: 1;
        }
      }
    </style>
  </head>
  <body>
    <div class="login-container">
      <div class="login-header">
        <i class="bx bxs-user-circle"></i>
        <h2>Admin Login</h2>
      </div>
      <% if (error) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="bx bxs-error"></i> Incorrect username or password
        <button
          type="button"
          class="btn-close"
          data-bs-dismiss="alert"
          aria-label="Close"
        ></button>
      </div>
      <% } %>
      <form action="/authenticate" method="post">
        <div class="form-floating mb-3">
          <input
            type="text"
            class="form-control"
            id="username"
            name="username"
            placeholder="Username"
            required
          />
          <label for="username"><i class="bx bxs-user"></i> Username</label>
        </div>
        <div class="form-floating mb-4">
          <input
            type="password"
            class="form-control"
            id="password"
            name="password"
            placeholder="Password"
            required
          />
          <label for="password"><i class="bx bxs-lock-alt"></i> Password</label>
        </div>
        <button type="submit" class="btn btn-login">
          <i class="bx bxs-log-in-circle"></i> Log In
        </button>
      </form>
    </div>
    <!-- Include Bootstrap JS and Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <%- include('footer') %>
  </body>
</html>
